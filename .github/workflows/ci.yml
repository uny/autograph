name: CI

permissions:
  contents: read

on:
  push:
    branches: [main]
  pull_request:
  # Manual trigger for the emulator smoke below, for PRs whose diff does not reach it (see `changes`).
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

# These independent chains run in parallel instead of adding their runtimes (~31 min previously).
# All require macOS for iosSimulatorArm64; Apple silicon runners execute its tests natively.

jobs:
  # Decides whether a PR's diff reaches the Android on-device smoke below. Only that job consults this;
  # every other job runs on every PR regardless. Path-filtering the workflow's `on.pull_request` instead
  # would silence all of them for an Android-only change, which is the opposite of what is wanted.
  #
  # `HEAD^1` on a pull_request checkout is the base branch tip (HEAD is the merge commit), so the diff
  # is exactly what the PR adds — no full clone, no third-party filter action.
  changes:
    name: changes
    runs-on: ubuntu-latest
    outputs:
      android: ${{ steps.filter.outputs.android }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        if: github.event_name == 'pull_request'
        with:
          persist-credentials: false
          fetch-depth: 2

      - id: filter
        if: github.event_name == 'pull_request'
        run: |
          set -euo pipefail
          android=false
          # ci.yml itself is in the set: a workflow edit must prove the emulator job still runs, on the
          # PR that edits it.
          if git diff --name-only HEAD^1 HEAD | grep -E '^(sample-android/|autograph-android/|\.github/workflows/ci\.yml$)' ; then
            android=true
          fi
          echo "android=$android" >> "$GITHUB_OUTPUT"

  # The one job here that guards something CI otherwise cannot reach. cd.yml runs only on a `v*`
  # tag, so the shell it depends on — the gate standing in front of an irreversible publish, and
  # the release-notes derivation behind it — would first execute during a real release; these
  # tests execute it on every PR instead. Seconds either way, and it is deliberately not folded
  # into `library` so a shell regression is not reported as a Kotlin failure.
  #
  # Both runners, because this shell only ever *executes* on macOS — cd.yml and release-dry-run.yml
  # are both `macos-latest` — while ubuntu is where a contributor's shell instincts come from.
  # Testing only on ubuntu would leave BSD `grep`/`sort` and bash 3.2 unexercised, so a GNU-only
  # construct added later (`grep -P`, `sed -i` without a suffix, `readlink -f`) would stay green
  # here and fail for the first time during the release this gate exists to protect.
  release-scripts:
    name: release-scripts (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Test the release version gate
        run: .github/scripts/validate-release-version.test.sh

      - name: Test the release notes start-tag derivation
        run: .github/scripts/derive-release-notes-start-tag.test.sh

  library:
    name: library
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: actions/setup-java@de7274f081f381c8f8158605e0321c36c376e2e6 # v6.0.1
        with:
          distribution: temurin
          java-version: '21'

      - uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6.3.0

      # Diagnostic only (see issue #23): linkDebugTestIosSimulatorArm64 intermittently fails
      # with "framework 'UIUtilities' not found" / undefined symbols, which looks like it
      # depends on which Xcode/SDK the runner resolves via `xcode-select` at build time. This
      # records that state so failures can be correlated against it.
      - name: Log active Xcode/SDK
        run: |
          xcode-select -p
          xcodebuild -version
          ls /Applications | grep -i '^Xcode'

      # Use `build` rather than `check` to cover device test sources and published artifacts.
      #
      # Skip sample framework assembly (~640s): CI cannot run the device frameworks, and sample-ios
      # below covers the simulator framework through Xcode's embed phase. Library artifacts, ABI
      # dumps, tests, and sample-shared Kotlin remain covered.
      #
      # The exclusion also removes autograph-compose's iosArm64 publication resources, so retain the
      # zip task used by cd.yml. The omitted `export*ApiElements` tasks are not part of publication.
      - name: Build and test all targets
        run: ./gradlew build -x :sample-shared:assemble :autograph-compose:iosArm64ZipMultiplatformResourcesForPublication --stacktrace

  swift-package:
    name: swift-package
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: actions/setup-java@de7274f081f381c8f8158605e0321c36c376e2e6 # v6.0.1
        with:
          distribution: temurin
          java-version: '21'

      - uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6.3.0

      # AutographSegmentSwift (root Package.swift) consumes Autograph.xcframework as a local
      # binaryTarget when present (see Package.swift), so it must be built before `xcodebuild test`
      # can link it.
      - name: Build Autograph.xcframework
        run: ./gradlew :autograph-apple:assembleAutographReleaseXCFramework --stacktrace

      - name: Test the Swift package (AutographSegmentSwift + AutographUI)
        run: |
          device=$(xcrun simctl list devices available -j | jq -r '.devices | to_entries | map(.value[] | select(.name | startswith("iPhone"))) | .[0].name')
          xcodebuild test -scheme Autograph-Package -destination "platform=iOS Simulator,name=$device"

  sample-ios:
    name: sample-ios
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: actions/setup-java@de7274f081f381c8f8158605e0321c36c376e2e6 # v6.0.1
        with:
          distribution: temurin
          java-version: '21'

      - uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6.3.0

      # The sample app links AutographUI (and so Autograph.xcframework) for its explicit SwiftUI
      # instrumentation screen — the only way to exercise the shipped `AutographButton` itself, since
      # it has no Kotlin half a facade could stand in for. Without this step Package.swift falls back
      # to downloading the last *released* umbrella, which predates that API and fails to compile.
      - name: Build Autograph.xcframework
        run: ./gradlew :autograph-apple:assembleAutographReleaseXCFramework --stacktrace

      # xcodebuild exercises sample-shared's embedAndSignAppleFrameworkForXcode path, which Gradle's
      # `build` does not. `test` also runs iosAppUITests for autocapture hit-testing behavior that
      # compose.uiTest cannot cover.
      - name: Test sample-ios
        run: |
          device=$(xcrun simctl list devices available -j | jq -r '.devices | to_entries | map(.value[] | select(.name | startswith("iPhone"))) | .[0].name')
          xcodebuild test -project sample-ios/iosApp.xcodeproj -scheme iosApp -destination "platform=iOS Simulator,name=$device"

  # The Android on-device smokes for #65's native screen capture and #63's native tap capture — the
  # coverage Robolectric cannot give (real framework, real windowing, a real back button, real touch
  # dispatch). Runs on Linux (KVM) since the task only needs the Android targets of its KMP
  # dependencies, not the iOS ones.
  #
  # An emulator job is slow, so it is gated by `changes` rather than run on every PR: it runs when the
  # PR touches sample-android/, autograph-android/, or this workflow, and on workflow_dispatch for
  # everything else. Not on push to main — the PR that landed the change already ran it.
  #
  # Know what the other jobs therefore do NOT cover: the Robolectric suite pins how a *given* pressed
  # hierarchy resolves, by setting `isPressed` by hand. That the framework's touch dispatch produces
  # that hierarchy in the first place — the premise the whole tap capture rests on — is only exercised
  # here.
  android-instrumented:
    name: android-instrumented
    needs: changes
    if: github.event_name == 'workflow_dispatch' || (github.event_name == 'pull_request' && needs.changes.outputs.android == 'true')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: actions/setup-java@de7274f081f381c8f8158605e0321c36c376e2e6 # v6.0.1
        with:
          distribution: temurin
          java-version: '21'

      - uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6.3.0

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Run the native screen and tap instrumented smokes
        uses: reactivecircus/android-emulator-runner@a421e43855164a8197daf9d8d40fe71c6996bb0d # v2.38.0
        with:
          api-level: 34
          arch: x86_64
          script: ./gradlew :sample-android:connectedDebugAndroidTest
