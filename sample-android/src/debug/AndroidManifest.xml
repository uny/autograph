<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Fixture hosts for OriginOnDeviceTest (debug only). They cannot live in the test APK: an
             Activity declared there resolves to the test package's process, and ActivityScenario
             refuses to launch across processes. -->
        <activity android:name=".OriginActivity" android:exported="false" />
        <activity android:name=".PagerActivity" android:exported="false" />
    </application>
</manifest>
